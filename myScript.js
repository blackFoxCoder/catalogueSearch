// JavaScript Document
function search() {
	var liquidSearch = $("#liquidSearch");
	var search = $("#search");
	if (liquidSearch !=='') {search.html('You are searching for ')}
	else {search.html('')};
}